FROM node:20
ARG BACKEND_API_URL_ARG
ENV BACKEND_API_URL=$BACKEND_API_URL_ARG
ARG BACKEND_AUTH_TOKEN_ARG
ENV BACKEND_AUTH_TOKEN=$BACKEND_AUTH_TOKEN_ARG
WORKDIR /app
COPY package.json yarn.lock ./
RUN yarn install
COPY . .
RUN yarn build
EXPOSE 4502
CMD ["yarn", "start"]